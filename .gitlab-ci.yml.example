image: node:4.2.2

variables:
  DEBIAN_FRONTEND: "noninteractive"

pages:
  cache:
    paths:
    - node_modules/
  script:
  - npm install -g grunt
  - npm install
  - npm run build
  artifacts:
    paths:
    - public
  only:
    - master
